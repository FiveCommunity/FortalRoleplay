Utils = {}

Utils.functions = {
    checkWanted = function(user_id)
        return false
        -- exports["black_hud"]:Wanted(user_id)
    end,
    sendLog = function(chestName,user_id,itemName,itemAmount)

    end 
}